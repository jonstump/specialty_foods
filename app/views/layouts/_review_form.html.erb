<%= render "layouts/errors", :object => @product %>

<h1>Enter a Review</h1>
<%= form_for [@product, @review] do |f| %>
  <%= f.label :author %>
  <%= f.text_field :author %>
  <%= f.label :rating %>
  <%= f.number :rating %>
  <p><%= f.submit %></p>

  <%= f.submit %>
<% end %>

